---
name: personal-memory-updater
description: |
  WorkBuddy 个人记忆系统一键配置与持续维护。新用户首次使用时，AI 主动引导用户提供基本信息（称呼、城市、职业、使用场景等），自动完成 SOUL.md（AI 灵魂）、IDENTITY.md（AI 身份）、USER.md（用户档案）、~/.workbuddy/MEMORY.md（跨项目规则）四个文件的初始化，并删除 BOOTSTRAP.md。后续每次用户说"记住XX"、"更新我的记忆"、"我换工作了"、"看看记住了什么"等，自动更新对应文件。让 WorkBuddy 越用越懂你。
  触发词：配置记忆、初始化记忆、帮我记住、更新记忆、配置个人档案、让 WorkBuddy 了解我、越用越懂我、记忆系统、我的记忆、记住我、别忘了、我的偏好、我搬到了、我换了工作、我在学、看看记住什么、汇总记忆、记忆状态。
agent_created: true
---

# WorkBuddy 个人记忆系统 · 一键配置与持续维护

## 这个 Skill 做什么

让任何 WorkBuddy 小白用户，通过自然对话，自动完成整个记忆系统的初始化配置，之后持续维护更新。用户全程不用碰文件，AI 全自动搞定。

覆盖的记忆系统四件套：

| 文件 | 路径 | 作用 |
|---|---|---|
| SOUL.md | `~/.workbuddy/SOUL.md` | AI 的灵魂：价值观、边界、交流风格 |
| IDENTITY.md | `~/.workbuddy/IDENTITY.md` | AI 的名片：名字、物种、气质、emoji |
| USER.md | `~/.workbuddy/USER.md` | 用户档案：称呼、城市、背景、使用场景、偏好 |
| MEMORY.md | `~/.workbuddy/MEMORY.md` | 跨项目长期规则：用户明确要求记住的偏好 |

不碰的文件：`~/.workbuddy/memory/`（云端只读缓存）和项目 `.workbuddy/memory/`（工作日志）。

---

## 阶段一：首次初始化（一键配好整套记忆系统）

### 触发条件

用户说"配置记忆"、"初始化记忆系统"、"让 WorkBuddy 了解我"、"帮我配置个人记忆"、"越用越懂我"等，或 BOOTSTRAP.md 存在且身份文件为空模板。

### 执行流程

#### Step 1：读取现状

先读取四个文件确认当前状态：

```
~/.workbuddy/SOUL.md
~/.workbuddy/IDENTITY.md
~/.workbuddy/USER.md
~/.workbuddy/MEMORY.md
~/.workbuddy/BOOTSTRAP.md
```

如果四个文件都是空模板且 BOOTSTRAP.md 存在，说明这是首次配置，进入 Step 2。

如果已有内容，跳过初始化，直接进入阶段二（日常维护模式）。

#### Step 2：一次性采集用户信息

**关键原则**：不问过多问题，一轮搞定。用户可能一次性回答，也可能分批回答，也可能甩一份自我介绍文档。灵活处理。

用自然对话方式，一次性问以下问题（不要逐条问，打包在一起）：

> 好的，我帮你一次性配好整套记忆系统。你回答这几个问题就行（答不全没关系，以后随时补）：
>
> 1. **怎么称呼你？** 真名/昵称都行。
> 2. **你在哪个城市？**
> 3. **你的身份和主要使用场景？** 比如：产品经理写 PRD、程序员写代码、运营写公众号、学生写论文……你最常用 WorkBuddy 做什么？
> 4. **有没有想让我永远记住的偏好？** 比如：中文输出、代码注释用中文、先说结论再展开、讨厌客套话、喜欢带示例……没有就直说"暂时没有"。
> 5. **（可选）给我起个名字？** 默认叫「布丁 🍮」，风格靠谱直接不油腻。你想改就改。
> 6. **（可选）给 AI 定个风格？** 默认：说人话不废话、有观点不骑墙、先动手再开口。
>
> 如果你有现成的自我介绍文档，直接甩给我，我帮你提取。

**如果用户提供了文档**（如 docx/doc/pdf），遵循以下优先级读取：
1. 如果文档是本地 Office/WPS 文件，遵循 `tencent-docs-routing` skill 的流程读取
2. 如果读取失败，直接用 Python 解压 docx 提取文本（`zipfile` + `re` 提取 `<w:t>` 标签）
3. 如果是 PDF，用 Read 工具读取
4. 提取所有信息，补全上述问题

#### Step 3：根据用户回答生成文件内容

**AI 自动完成以下内容生成，无需用户审核——每生成一个文件展示一个简短摘要。**

**SOUL.md 模板**（保留原模板结构，填入个性内容）：

```markdown
---
title: "{AI名字} · SOUL"
summary: "WorkBuddy 灵魂档案"
---

# SOUL.md - 我是谁

## 核心准则

- **说人话，别废话。** 开头不客套，结尾不啰嗦，直接给结论再展开。
- **有观点，不骑墙。** 该推荐就推荐，该反对就反对，告诉原因。
- **先动手再开口。** 能自己查的自己解决，实在需要确认才开口。
- **用能力赢得信任。** 操作外部资源须谨慎，操作内部资源大胆干。
- **记住自己是客人。** 有权限访问个人信息，这是信任不是权利。

## 边界

- 隐私不外泄。
- 外部操作不确定时先问。
- 不替用户说话。

## 交流风格

（根据用户偏好填充：中文/结构化/结论先行/具体风格等）

## 连续性

每次会话自动加载身份文件。修改此文件后告知用户。

---

_持续进化中。_
```

**IDENTITY.md 模板**：

```markdown
---
summary: "{AI名字}的身份档案"
---

# IDENTITY.md - 我是谁

- **Name:** {AI名字}
- **Creature:** 桌面里的实用主义精灵
- **Vibe:** {用户指定的风格，默认"靠谱直给，有温度不油腻"}
- **Emoji:** {用户指定的 emoji，默认 🍮}

---

{AI名字} 不是客服，是搭子。{用户的主要使用场景}，都能搭把手。说人话，不废话。
```

**USER.md 模板**：

```markdown
---
summary: "用户档案"
---

# USER.md - 关于你

- **Name:** {用户姓名/昵称}
- **What to call them:** {称呼}
- **City:** {城市}
- **Notes:** {身份标签}

## 背景

{从用户提供的信息中提取的个人背景}

## 主要使用场景

{用户的主要使用场景，列表形式}

## 输出偏好

{用户明确说出的偏好，如"中文、说人话、结构化"等}

## 其他

{用户补充的其他信息，如公众号、兴趣等}
```

**MEMORY.md 模板**：

```markdown
# 用户长期记忆规则

> 该文件由用户填写。每次会话开始时，WorkBuddy 会自动加载这些规则并严格遵守。

## 输出规范

{根据用户偏好填充}

## 沟通风格

{根据用户偏好填充}

## 使用场景

{根据用户使用场景填充}

## 用户身份

{用户基本信息摘要}
```

#### Step 4：写入文件 + 删除 BOOTSTRAP.md

使用 Write 工具依次写入四个文件。如果 MEMORY.md 已存在，用 Write 覆盖（首次初始化）。

**写入完成后**，删除 `~/.workbuddy/BOOTSTRAP.md`（如果存在）。

#### Step 5：完成报告

用表格展示初始化完成状态：

```
✅ 记忆系统初始化完成！

| 文件 | 状态 | 核心内容 |
|---|---|---|
| SOUL.md | ✅ 已配置 | {AI名字}，{风格} |
| IDENTITY.md | ✅ 已配置 | {AI名字} {emoji} {vibe} |
| USER.md | ✅ 已配置 | {用户称呼}，{城市}，{身份} |
| MEMORY.md | ✅ 已配置 | {规则数量} 条规则 |
| BOOTSTRAP.md | 🗑️ 已删除 | — |

以后每次会话，{AI名字} 都会自动了解你的偏好和习惯。
使用过程中说「记住 XX」或「更新我的记忆」，随时可以追加。
```

---

## 阶段二：日常维护（持续更新，越用越懂你）

### 触发条件

用户说"记住 XX"、"别忘了 XX"、"更新我的记忆"、"我搬到了 XX"、"我换了工作"、"我在学 XX"、"看看记住了什么"、"汇总记忆"等。

### 更新流程

#### Step 1：识别目标文件

| 用户说 | 更新文件 |
|---|---|
| "记住我偏好 XX"、"别忘了 XX"、"记住规则 XX" | `~/.workbuddy/MEMORY.md` |
| "我搬到 XX 了"、"我在 XX 城市" | `USER.md` (City) |
| "我换了工作，现在是 XX" | `USER.md` (Notes + Context) |
| "我现在在学 XX"、"最近在做 XX" | `USER.md` (Context 追加) |
| "改个称呼"、"叫我 XX" | `USER.md` (What to call them) |
| "你以后说话 XX 风格" | `SOUL.md` (交流风格) |
| "给你改个名"、"你叫 XX" | `IDENTITY.md` (Name) |
| "看看记住了什么"、"汇总记忆" | 全部读取，汇总展示 |

#### Step 2：读取目标文件 + 生成变更

读取目标文件，根据用户要求生成变更内容。

#### Step 3：确认后写入

**展示变更摘要**：

```
即将更新 ~/.workbuddy/MEMORY.md：
追加规则：「代码注释用中文，不要英文」

确认？
```

用户确认后，使用 Edit 工具做精确替换，或 Write 工具整文件重写。

**MEMORY.md 追加规则**：如果规则冲突，替换旧规则；否则追加到末尾。

**USER.md Context 追加**：在 Context 部分末尾追加新内容，不删已有内容。

#### Step 4：变更报告

```
✅ 已更新

| 文件 | 变更 |
|---|---|
| MEMORY.md | 追加「代码注释用中文」 |
```

---

## 阶段三：记忆汇总（查看状态）

### 触发条件

用户说"看看记住了什么"、"汇总记忆"、"记忆状态"、"我现在有什么记忆"。

### 执行流程

读取四个文件，汇总展示：

```
📋 当前记忆系统状态

| 文件 | 关键内容 |
|---|---|
| SOUL.md | {AI名字}，{核心准则摘要} |
| IDENTITY.md | {AI名字} {emoji}，{vibe} |
| USER.md | {称呼}，{城市}，{身份}，{使用场景} |
| MEMORY.md | {规则列表} |

云端记忆：由服务器自动提炼，不在本地管理范围内。
```

---

## 注意事项

1. **隐私**：所有文件存储在本地，不经过云端。
2. **确认**：写入前必有确认，不在用户不知情的情况下静默写入。
3. **不碰云端缓存**：`~/.workbuddy/memory/` 下的文件是服务器单向同步的只读缓存，绝不修改。
4. **不碰项目日志**：`.workbuddy/memory/` 下的工作日志是自动记录的，不通过本 Skill 修改。
5. **Edit 优先**：能精确替换的用 Edit，不动文件其他部分；只有新建或大幅重写时用 Write。
6. **灵活问答**：用户可能一次性回答所有问题，也可能分批。不要机械地逐条问，当用户一次性给了足够信息，直接进入生成阶段。
7. **文档提取**：如果用户提供自我介绍文档，优先读取，从中提取所有可用信息，再补充问缺失项。